#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

exec node --experimental-strip-types "$PROJECT_ROOT/scripts/cli/install-codex-plugin.ts" "$@"
